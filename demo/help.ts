let listener: ((id?: string) => void) | undefined

export function openHelp(sectionId?: string) {
  listener?.(sectionId)
}

export function onOpenHelp(fn: (id?: string) => void) {
  listener = fn
}

let trialListener: ((text: string) => void) | undefined

export function startTrial(text: string) {
  trialListener?.(text)
}

export function onStartTrial(fn: (text: string) => void) {
  trialListener = fn
}

export const TUTORIAL_TEXT = `# horkewへようこそ！
# このサイトは、人狼ゲームのメモをテキスト形式で作成し解析するためのツールです。
# テキストで時間順に事実を記述することで、ゲームの流れそのものを保存することが可能になります。

# まずはこのエディタペインにフォーカスを移動し、カーソルキーでカーソルをCOの行まで移動してみましょう。

# 配役: 村人2, 占い師1, 人狼1, 狂人1
配役 村2 占1 狼1 狂1

# 参加者を登録
# 行頭の「++」は参加者の一括登録。「+アリス」のように「+」一つで1人ずつ登録もできます。
++アリス ボブ チャーリー デイブ エミリー

# 1日目: CO（カミングアウト＝役職の宣言）と投票
# 占い結果は「白 / ○」（人間）、「● / 黒」（人狼）のどちらの表記でも書けます。
アリス: 占いCO ボブ白
デイブ: 占いCO チャーリー●

# 画面中央に「CO表」が表示されました。
# * ゲームの状態は、現在カーソルがある行までだけが反映されます。

# 投票は右向きの矢印で順番に記述します。
アリス → チャーリー
ボブ → チャーリー
チャーリー → デイブ
デイブ → ボブ
エミリー → チャーリー

チャーリー処刑

# 右下の盤面表に注目してください。
# ここまでのCO状況と、ゲームがまだ続いているということから、自動的にアリスが真占い、デイブは偽だということがわかります。
# （※ このツールは「対抗を無視した潜伏」は前提としていません。真占いがいれば対抗COに出る想定です）

# 2日目朝: 夜の襲撃結果
襲撃 デイブ

# 2日目: COと投票
# 2日目以降は「N日目 対象 結果」の形で占い結果だけを書けます（「占いCO」の宣言は初日だけで十分）。
アリス: 2日目 エミリー●

ボブ → エミリー
アリス → エミリー
エミリー → ボブ

エミリー処刑

# 最後にゲームの結末を書いてください。
# 結末は「村勝ち」「狼勝ち」「狐勝ち」「引き分け」などが書けます。
村勝ち

# これでチュートリアルは終わりです。
# 新しいゲームを作るには画面上部の「新規作成」ボタンから。
# 詳しい記法は画面右上の「?」ボタンからヘルプパネルを開いてください。
`
